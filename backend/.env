# Database (fallbacks to SQLite if not provided)
DB_NAME=org
DB_USER=postgres
DB_PASSWORD=1234
DB_HOST=localhost
DB_PORT=5432

# Allowed origin for frontend
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
